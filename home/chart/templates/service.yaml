apiVersion: v1
kind: Service
metadata:
  name: {{ include "home.fullname" . }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ include "home.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 80
